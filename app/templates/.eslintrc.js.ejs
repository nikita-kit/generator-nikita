/*
 * eslint config for IDEs
 * please do NOT modify this file!
 * instead, change options and rules at grunt eslint config
 * @see grunt/config/eslint.js
 */
const gruntEslintConfig = require('./grunt/config/eslint').options;
const config = gruntEslintConfig.baseConfig;

config.rules = gruntEslintConfig.rules;
config.parser = gruntEslintConfig.parser;
config.parserOptions = gruntEslintConfig.parserOptions;

module.exports = config;
