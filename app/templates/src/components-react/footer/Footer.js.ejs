import React, { Component } from 'react';

class Footer extends Component {

    render() {
        return (
            <footer className="b-footer">
                nikita kickstarter <%= config.get('version') %> - <%= config.get('template') %> - <%= config.get('jsFramework') %>
            </footer>
        );
    }
}

export default Footer;
