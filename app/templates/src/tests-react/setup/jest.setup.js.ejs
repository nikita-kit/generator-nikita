/*
 * jest setup file
 * runs before every test file
 */
import { configure } from 'enzyme';
import Adapter from 'enzyme-adapter-react-16';

/* setup enzyme */
configure({ adapter: new Adapter() });

/* mock console.log */
console.log = jest.fn();

/* mock window.matchMedia */
window.matchMedia = jest.fn().mockImplementation(query => ({
    matches: true,
    media: query,
    onchange: null,
    addListener: jest.fn(),
    removeListener: jest.fn(),
}));

/* setup media querys */
document.head.innerHTML = '<meta class="foundation-mq" style="font-family:\'xsmall=0em&small=21.875em&medium=40em&large=59.375em&xlarge=78.75em\'">';
