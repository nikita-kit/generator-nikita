module.exports = {
    dev: {
        options: {
            outputStyle: 'nested',
            sourceMap: true,
            includePaths: ['<%%= paths.src %>/sass']
        },
        files: {
            '<%%= paths.dev %>/generated/styles.css': '<%%= paths.tmp %>/styles.scss'
        }
    },
    dist: {
        options: {
            outputStyle: 'nested', // minifying by cssmin-task
            sourceMap: false,
            includePaths: ['<%%= paths.src %>/sass']
        },
        files: {
            '<%%= paths.dist %>/generated/styles.css': '<%%= paths.tmp %>/styles.scss'
        }
    }
};
