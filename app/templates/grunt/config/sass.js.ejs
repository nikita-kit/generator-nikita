module.exports = {
    dev: {
        options: {
            outputStyle: 'nested',
            sourceMap: true,
            includePaths: ['<%%= paths.src %>/sass']
        },
        files: [{
            expand: true,
            cwd: '<%%= paths.tmp %>',
            dest: '<%%= paths.dev %>/generated',
            src: ['*.scss'],
            ext: '.css'
        }]
    },
    dist: {
        options: {
            outputStyle: 'nested', // minifying by cssmin-task
            sourceMap: false,
            includePaths: ['<%%= paths.src %>/sass']
        },
        files: [{
            expand: true,
            cwd: '<%%= paths.tmp %>',
            dest: '<%%= paths.dist %>/generated',
            src: ['*.scss'],
            ext: '.css'
        }]
    }
};
