module.exports = {
    options: {
        cache: false,
        coverageDirectory: 'reports/coverage',
        testMatch: [
            '<rootDir>/<%%= paths.src %>/tests/**/*.test.js?(x)',
            '<rootDir>/<%%= paths.src %>/components/**/*.test.js?(x)',
        ],
        transform: {
            '^.+\\.js$': '<rootDir>/<%%= paths.src %>/tests/jest.transform.js',<% if (config.get('jsFramework') === 'jsb') { %>
            '^.+\\.ejs$': '<rootDir>/<%%= paths.src %>/tests/jest.transform-ejs.js',<% } %>
        },
        setupTestFrameworkScriptFile: '<rootDir>/<%%= paths.src %>/tests/jest.setup.js',
        transformIgnorePatterns: ['/node_modules/'],
    },
    test: {
        options: {
            collectCoverage: false,
        },
    },
    testCoverage: {
        options: {
            collectCoverage: true,
        },
    }
};
