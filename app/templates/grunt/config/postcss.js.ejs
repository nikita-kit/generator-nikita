module.exports = function (grunt, options) {
    return {
        options: {
            processors: [
                require('autoprefixer')({
                    browsers: options.browserslist
                })
            ]
        },
        dev: {
            options: {
                map: true
            },
            src: '<%%= paths.dev %>/generated/*.css'
        },
        dist: {
            src: '<%%= paths.dist %>/generated/*.css'
        }
    }
};
