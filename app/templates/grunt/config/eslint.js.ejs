module.exports = {
    options: {
        envs: ['browser', 'jest'],
        configFile: './node_modules/eslint-config-nikita/index.js',<% if (config.get('jsFramework') === 'react') { %>
        plugins: ['react'],
        baseConfig: {
            extends: [
                'plugin:react/recommended'
            ]
        },
        parser: 'babel-eslint',
        parserOptions: {
            ecmaVersion: 2018,
            ecmaFeatures: {
                jsx: true,
            }
        },<% } %>
        rules: {
            // override rules here<% if (config.get('jsFramework') === 'react') { %>
            'react/prop-types': 0,<% } %>
        },
    },
    check: [
        '<%%= paths.src %>/js/**/*.js',
        '<%%= paths.src %>/components/**/*.js',
        '<%%= paths.src %>/tests/**/*.test.js',
        '<%%= paths.src %>/components/**/*.test.js',
    ],
    fix: {
        options: {
            fix: true,
        },
        src: [
            '<%%= paths.src %>/js/**/*.js',
            '<%%= paths.src %>/components/**/*.js',
            '<%%= paths.src %>/tests/**/*.test.js',
            '<%%= paths.src %>/components/**/*.test.js',
        ]
    }
};
