module.exports = {
    options: {
        envs: ['browser', 'jest'],
        configFile: './node_modules/eslint-config-nikita/index.js',
        rules: {
            // override rules here
        }
    },
    check: ['<%%= paths.src %>/js/**/*.js', '<%%= paths.src %>/tests/**/*.test.js'],
    fix: {
        options: {
            fix: true,
        },
        src: ['<%%= paths.src %>/js/**/*.js', '<%%= paths.src %>/tests/**/*.test.js']
    }
};
