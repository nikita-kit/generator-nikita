module.exports = {
    fonts: {
        cwd: '<%%= paths.src %>/fonts/',
        dest: '<%%= paths.dist %>/fonts/',
        expand: true,
        src: ['**/*']
    },
    images: {
        cwd: '<%%= paths.src %>/img/',
        dest: '<%%= paths.dist %>/img/',
        expand: true,
        src: ['**/*.{jpg,jpeg,png,gif,ico}', '!dev/**/*']
    }<% if (config.get('features').indexOf('cssStyleGuide') != -1) { %>,
    styleguide: {
        cwd: '<%%= paths.dev %>/css/',
        dest: 'styleguide/css/',
        expand: true,
        filter: 'isFile',
        flatten: true,
        src: ['**/*.css']
    }<% } %>
};
