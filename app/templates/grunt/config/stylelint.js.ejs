module.exports = {
    options: {
        configFile: './node_modules/stylelint-config-nikita/index.js',
        formatter: 'string',
        syntax: 'scss',
        configOverrides: {
            rules: {
                // override rules here
            }
        }
    },
    check: ['<%%= paths.src %>/sass/**/*.scss'],
    fix: {
        options: {
            fix: true,
        },
        src: ['<%%= paths.src %>/sass/**/*.scss'],
    }
};
