module.exports = {

    // standard build task when calling grunt without arguments
    'default': [
        'build',
    ],

    // development build task
    'dev': [
        'clean:static',
        'clean:tmp',<% if (config.get('features').includes('svgBackgrounds')) { %>
        'svgmin:src_bg',
        'svgcss',
        'string-replace',<% } %>
        'sass-globbing',
        'concurrent:dev',
        'postcss:dev',
        'webpack:dev',
        'sync',
    ],

    // development build task including server and watch
    'build': [
        'dev',
        'browserSync:dev',
        'watch',
    ],

    // distribution task
    'dist': [
        'clean:static',
        'clean:tmp',<% if (config.get('features').includes('svgBackgrounds')) { %>
        'svgmin:src_bg',
        'svgcss',
        'string-replace',<% } %>
        'sass-globbing',
        'sass:dist',<% if (config.get('features').includes('gitinfos')) { %>
        'gitinfo',
        'write-gitinfos',<% } %>
        'twigRender:dist',
        'postcss:dist',
        'webpack:dist',
        'sync',
        'uglify',
        'prettify:static',
    ],

    // testing, linting and fixing tasks

    'test': [
        'jest:test',
    ],

    'test-coverage': [
        'jest:testCoverage',
    ],

    'minify-images': [
        'imagemin:static',
        'svgmin:static',
    ],

    'check-scss': [
        'stylelint:check',
    ],

    'fix-scss': [
        'stylelint:fix',
    ],

    'check-js': [
        'eslint:check',
    ],

    'fix-js': [
        'eslint:fix',
    ],

    'analyze-js': [
        'webpack:analyze',
    ],
};
